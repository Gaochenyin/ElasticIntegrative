#' Simulation results for beta0 = c(0,1,1,1) and psi0 = c(0,1,1) in Yang et al., (2022), Section 4
#'
#' @format `elastic_psi011_lists`
#' A data list with length 10,
#' each one associated with different strength of unmeasured confounding (e.g., 0, ..., 2)
"elastic_psi011_lists"


#' Simulation results for beta0 = c(0,1,1,1) and psi0 = c(0,0,0) in Yang et al., (2022), Section 4
#'
#' @format `elastic_psi000_lists`
#' A data list with length 10,
#' each one associated with different strength of unmeasured confounding (e.g., 0, ..., 2)
"elastic_psi000_lists"



#' Simulation results for beta0 = c(0,1,1,1) and psi0 = c(0,0,0) in Yang et al., (2022), Section S4.5
#'
#' @format `elastic_psi000_fixed_lists`
#' A data list with length 10,
#' each one associated with different strength of unmeasured confounding (e.g., 0, ..., 2)
"elastic_psi000_fixed_lists"


#' Simulation results for beta0 = c(0,1,1,1) and psi0 = c(0,1,1) in Yang et al., (2022), Section S4.5
#'
#' @format `elastic_psi011_fixed_lists`
#' A data list with length 10,
#' each one associated with different strength of unmeasured confounding (e.g., 0, ..., 2)
"elastic_psi011_fixed_lists"


#' Simulation results for beta0 = c(0,1,1,1) and psi0 = c(0,1,1) in Yang et al., (2022), Section S4.1
#' with weak separation
#' @format `elastic_aipw_ses_l1_list`
#' A data list with no unmeasured confounding
"elastic_aipw_ses_l1_list"


#' Simulation results for beta0 = c(0,1,1,1) and psi0 = c(0,1,1) in Yang et al., (2022), Section S4.1
#' with median separation
#' @format `elastic_aipw_ses_l2_list`
#' A data list with no unmeasured confounding
"elastic_aipw_ses_l2_list"


#' Simulation results for beta0 = c(0,1,1,1) and psi0 = c(0,1,1) in Yang et al., (2022), Section S4.1
#' with strong separation
#' @format `elastic_aipw_ses_l3_list`
#' A data list with no unmeasured confounding
"elastic_aipw_ses_l3_list"

